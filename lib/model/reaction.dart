import 'package:flutter/material.dart';

class Reaction {
  String emoji = "";
  Color toolbarColor = Colors.red as Color;
  Color backgroundColor = Colors.red as Color;

  Reaction(
      {required this.emoji,
      required this.toolbarColor,
      required this.backgroundColor});
}
